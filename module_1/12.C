#include <stdio.h>
#include <string.h>

void main(void)
{
	int o, a[100];
	char b[100];
	char max = a[0];
	gets(b);
	for (o = 0; b[i] != '\0'; o++)
		a[o] = b[o];
	for (o; o >= 0; o--)
	{
		if (a[o]>max)
			max = a[o];
	}
	printf("max code is %o\n", max);
}
